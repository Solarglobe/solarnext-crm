/**
 * domains/pv-catalog/index.js — Barrel export du domaine pv-catalog.
 * Panneaux, onduleurs, batteries — catalogue admin.
 *
 * Ce fichier exporte le router Express du domaine.
 * Importer depuis httpApp.js via :
 *   import pv-catalogRouter from "./domains/pv-catalog/index.js";
 *   app.use("/api/pv-catalog", pv-catalogRouter);
 *
 * Migration progressive : les anciens chemins (routes/pv-catalog.routes.js)
 * restent en place comme stubs de réexportation pendant la transition.
 */

// TODO: exporter le router une fois migré
// export { default } from "./pv-catalog.router.js";
