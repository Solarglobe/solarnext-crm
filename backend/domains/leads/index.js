/**
 * domains/leads/index.js — Barrel export du domaine Leads.
 *
 * CRUD leads, Kanban, assignation, scoring, conversion client.
 * Migré depuis routes/leads.routes.js (Step #8 — DDD léger).
 */

export { default } from "./leads.router.js";
