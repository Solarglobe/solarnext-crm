/**
 * domains/invoices/index.js — Barrel export du domaine invoices.
 * Factures, paiements, avoirs, relances.
 *
 * Ce fichier exporte le router Express du domaine.
 * Importer depuis httpApp.js via :
 *   import invoicesRouter from "./domains/invoices/index.js";
 *   app.use("/api/invoices", invoicesRouter);
 *
 * Migration progressive : les anciens chemins (routes/invoices.routes.js)
 * restent en place comme stubs de réexportation pendant la transition.
 */

// TODO: exporter le router une fois migré
// export { default } from "./invoices.router.js";
