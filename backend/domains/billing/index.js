/**
 * domains/billing/index.js — Barrel export du domaine billing.
 * Stripe, plans, webhooks (Phase 6).
 *
 * Ce fichier exporte le router Express du domaine.
 * Importer depuis httpApp.js via :
 *   import billingRouter from "./domains/billing/index.js";
 *   app.use("/api/billing", billingRouter);
 *
 * Migration progressive : les anciens chemins (routes/billing.routes.js)
 * restent en place comme stubs de réexportation pendant la transition.
 */

// TODO: exporter le router une fois migré
// export { default } from "./billing.router.js";
