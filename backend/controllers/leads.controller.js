/**
 * controllers/leads.controller.js — STUB de réexportation (Step #8 — DDD léger).
 *
 * Le controller réel est dans domains/leads/leads.controller.js.
 * Ce stub maintient la rétrocompatibilité pour les imports existants.
 *
 * NE PAS MODIFIER — modifier domains/leads/leads.controller.js à la place.
 */

export * from "../domains/leads/leads.controller.js";
