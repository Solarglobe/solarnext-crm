{
  "name": "solarnext-crm",
  "private": true,
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "test:e2e": "npm --prefix frontend exec -- playwright test -c playwright.config.ts",
    "restore:storage:railway": "node backend/scripts/railway-restore-storage.mjs",
    "push:github": "git push -u origin HEAD",
    "push:github:tags": "git push origin --tags"
  }
}
