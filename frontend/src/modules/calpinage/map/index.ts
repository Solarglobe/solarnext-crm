/**
 * MapProvider — Interface et types pour les providers cartographiques.
 * Implémentations : map-selector-bundle.js (Google, Ortho/Leaflet).
 */
export type {
  MapProvider,
  MapInitOptions,
  MapEvent,
  CaptureResult,
} from "./MapProvider.types";
