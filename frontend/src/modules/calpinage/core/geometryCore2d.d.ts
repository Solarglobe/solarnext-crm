/**
 * Typages pour le module JS `geometryCore2d.js` (JSDoc source).
 */
export function pointInPolygon2d(
  pt: { x: number; y: number },
  poly: ReadonlyArray<{ x: number; y: number }>,
): boolean;

declare module "*.js";
