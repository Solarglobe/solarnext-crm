// ============================================================================
// ENGINE-P10 — P10 rendu par React (PdfLegacyPort/PdfPage10). Stub conservé
// pour compatibilité de chargement ; aucune mutation DOM.
// ============================================================================
(function () {
  window.EngineP10 = {
    bind: function () {},
  };
})();
